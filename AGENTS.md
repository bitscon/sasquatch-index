# AGENTS.md — Sasquatch Index

**The project contract is [`SASQUATCH_OS.md`](SASQUATCH_OS.md). Read it end to end
before acting; it governs everything about how this project is run.** This file
exists to satisfy the workspace bootstrap standard and to record how workspace
governance applies here.

## How workspace law applies to this repo

- This project is registered in the workspace registry (AGENT_OS.md, Section 17)
  as `static-web`, deployed by GitHub Pages — never by VPS rsync.
- **Record-keeping exemption (owner-granted standing rule, 2026-08-29, reaffirmed
  2026-09-02):** this project's durable record is `HANDOFF.md` plus the git
  history. Per-task Change Records under `docs/changes/` are waived. `HANDOFF.md`
  is overwritten at the end of every session using the exact template in
  SASQUATCH_OS.md section 9.
- **No Kanboard board** (owner decision 2026-08-29). The phase plan lives in
  SASQUATCH_OS.md section 8; session state lives in `HANDOFF.md`.
- One phase per session. State the plan and its risk rating, wait for the owner's
  approval, then run without check-ins; interrupt only on divergence.

## Hard rules (from SASQUATCH_OS.md — do not re-derive them, read it)

- Nothing is sold to visitors. No scraping — affiliate feeds only. The site runs
  unattended. Phone-and-voice first.
- Never fabricate first-hand experience, in any voice.
- The owner is not a content source; never prompt him for fit notes or reviews.
- Do not build apparel yet; keep the data model and URLs ready for it.
